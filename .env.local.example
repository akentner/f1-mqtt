# Local Environment Overrides
# This file overrides values from .env and should not be committed to git
# Copy this file to .env.local and customize for your local development

# Example: Override MQTT broker for local development
# MQTT_BROKER_URL=mqtt://192.168.1.100:1883
# MQTT_USERNAME=local_user
# MQTT_PASSWORD=local_password

# Example: Custom retained topics configuration (MQTT standard patterns)
# Comma-separated list using MQTT wildcards: + (single level), # (multi-level)
# MQTT_RETAINED_TOPICS=sessioninfo,driverlist,weather/#,+/status,timing/+/sector#

# Example: Enable session recording for local testing
# SESSION_RECORDING_ENABLED=true
# SESSION_RECORDING_MODE=hybrid
# SESSION_RECORDING_AUTO_START=true

# Example: Enable debug logging locally
# LOG_LEVEL=debug
# SIGNALR_MESSAGE_LOGGING=true

# Example: Use different F1 endpoints for testing
# F1_NEGOTIATE_URL=http://localhost:8080/signalr/negotiate
# F1_CONNECT_URL=ws://localhost:8080/signalr/connect

# Example: Custom MQTT topic prefix
# MQTT_TOPIC_PREFIX=f1_dev

# Example: Different health server port
# SERVER_PORT=3001
